# .dockerignore for C# / .NET services
# Place in: src/cartservice/

# Build output — compiled binaries (100MB+ per target framework)
**/obj/
**/bin/
**/publish/

# NuGet package cache — restored via dotnet restore inside Docker
packages/
*.nupkg
*.snupkg
.nuget/
project.lock.json
project.fragment.lock.json

# Test results
TestResults/
[Tt]est[Rr]esult*/
*.trx
coverage.cobertura.xml
coverage.opencover.xml

# IDE
.vscode/
.idea/
.vs/
*.suo
*.user
*.userosscache
*.rsuser

# OS
.DS_Store
Thumbs.db
desktop.ini
*.swp

# Note: *.csproj MUST be included (project definition)
# Note: *.sln MUST be included if building from solution
# Note: src/ and tests/ MUST be included
