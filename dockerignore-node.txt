# .dockerignore for Node.js services
# Place in: src/currencyservice/, src/paymentservice/

# Dependencies — installed via npm ci inside Docker
node_modules/
.npm/
.yarn/
yarn.lock     # if using npm, exclude yarn.lock (and vice versa)

# Build output
dist/
build/
.next/
out/

# Test artifacts
coverage/
.nyc_output/
*.lcov
jest-results.xml

# Linting cache
.eslintcache
.stylelintcache

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Note: package.json MUST be included
# Note: package-lock.json MUST be included (npm ci requires it)
# Note: *.js source files and proto/ directory MUST be included
